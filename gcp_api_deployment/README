# Déploiement d’une API ML sur Google Cloud Run

Ce projet consiste à déployer un modèle de Machine Learning
via une API FastAPI containerisée avec Docker et déployée sur Cloud Run.

## Stack
- FastAPI
- Docker
- Google Cloud Run

## Endpoints
- `/` : test API
- `/predict` : prédiction

## Déploiement
```bash
gcloud builds submit --tag gcr.io/<project-id>/ml-api
gcloud run deploy ml-api --allow-unauthenticated

---

